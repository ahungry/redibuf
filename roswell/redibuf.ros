#!/bin/sh
#|-*- mode:lisp -*-|#
#| A skelly project for Common Lisp dependant on SBCL, generated by Ahungry Fleece
exec ros -Q -L sbcl-bin -- $0 "$@"
|#
#-sbcl
(error "redibuf requires SBCL. Make sure it is installed with `ros install sbcl' and use it with `ros use sbcl'.")

(progn ;;init forms
  (ql:quickload '(#:redibuf) :silent t))

(defpackage #:ros.script.redibuf
  (:use #:cl))

(in-package :ros.script.redibuf)

(defun main (&rest argv)
  (redibuf:main argv))

;;; vim: set ft=lisp lisp:
